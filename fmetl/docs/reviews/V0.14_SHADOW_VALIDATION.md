# fmetl v0.14 七日影子验证

> 数据范围：A3XV，2026-07-17 至 2026-07-23  
> 数据来源：字段手册登记的 Hive→StarRocks 镜像；商品集、加工关系和特殊损耗为辅助关系／展示证据  
> 运行边界：只写本地 DuckDB；服务器写入次数为 0  
> 结论：本地影子账本通过全部硬门禁；尚未获得生产发布授权

## 1. 本次产出

- `t_v014_levels_result`：122,061 行，覆盖 7 个业务日。
- 输出合同：v1.5 兼容 123 字段，末尾追加 `article_group_id`、
  `article_group_name`，共 125 字段。
- SKU 层：59,836 行；商品集层：46,409 行。
- 内部保留：关系登记、关系解析、内部过账、SKU 日账、异常、运行清单和验证结果。
- 包版本：`0.14`；源码直接落在现有 `fmetl/`，没有单独建立 v014 代码目录。

## 2. 硬门禁

以下 8 项均通过，失败数均为 0：

| 校验 | 结果 |
|---|---|
| 外部观察量守恒 | 销售、退回、验收和已知损耗原样进入 SKU 日账 |
| SKU 数量守恒 | 数量方程残差在容差内为 0 |
| SKU 金额守恒 | 金额方程残差在容差内为 0 |
| 内部金额守恒 | 每个内部事件转出金额等于转入金额 |
| BOM 父品全部转出 | 正式 BOM 父品期末数量为 0 |
| 跨日连续 | D+1 期初数量和金额精确复制 D 日期末 |
| 负库存保护 | 无负期末数量 |
| 负库存金额保护 | 无负期末金额跨日 |

全量单元测试：137 个通过，0 个失败。

## 3. 关系处理结果

| 关系类型 | 状态 | 关系日记录数 |
|---|---|---:|
| 同码 | 正式 | 20,513 |
| BOM | 正式 | 3,014 |
| 显式换码／包装 | 正式 | 341 |
| 加工 | 正式 | 189 |
| 商品集候选 | 待转换事实 | 5,205 |
| 未识别 | 隔离 | 31,705 |

关系隔离不等于数据丢失。没有方向、数量、比例或生效证据的关系不生成内部库存流，
原始销售、验收、盘点和损耗仍保留在各自 SKU 日账。候选和未识别关系需逐步补证据，
不能由同商品集或同物料码自动造流。

## 4. 与当前 v1.5 的只读诊断

v1.5 只作输出和差异诊断，不作为 v0.14 库存、成本、损耗或毛利输入。

- SKU 销售金额和销售数量全部一致，失败记录为 0。
- 门店日验收金额全部一致，失败记录为 0。
- 4,880 个发生销售的 SKU 日大分类全部一致，证明 v0.14 使用当前 v1.5 大分类口径。
- 门店毛利：v0.14 为 32,894.89 元，v1.5 为 32,941.90 元，差 -47.02 元，
  差异率 -0.14%。
- 供应链毛利：v0.14 为 9,885.05 元，v1.5 为 9,883.64 元，差 1.41 元，
  差异率 0.01%。
- 全链路毛利：v0.14 为 42,779.94 元，v1.5 为 42,825.55 元，差 -45.61 元，
  差异率 -0.11%。
- 门店及大分类周毛利硬门禁失败数为 0。水饮类 -92.37 元和冷藏加工及预制菜类
  +32.00 元超过单类 2%，但分别能由正式包装换码成本和跨分类内部过账完整解释，
  标记为 `EXPECTED_RELATION_DELTA`，不作为计算错误放行。
- 冻结的 123 个参考字段均进入字段矩阵。其中 16 个字段未由 v1.5 查询接口返回，
  标记为 `REFERENCE_UNAVAILABLE`；禁止将缺失参考值当作 0。其余字段仅作差异诊断，
  不以逐字段趋同替代 v0.14 的关系、数量和金额守恒。

## 5. 特殊损耗边界

- 炒菜机和生熟联动不改写 `v014_sku_daily` 的库存、成本或会计毛利。
- 只在 125 字段输出层应用 v1.5 兼容展示：
  - 炒菜机金额从损耗扣除并加回来源 SKU 毛利。
  - 生熟联动金额加回来源大类，同时从熟食类扣回；门店总毛利净影响为 0。
- 特殊展示字段与会计底项分离，避免为对齐报表反向污染库存账本。

## 6. 发布前剩余事项

- 业务确认 A3XV 商品集 `area_name IS NULL` 通用映射。
- 补齐未确认牛肉 BOM、蔬菜固定转码和乳品包装比例。
- 对高金额隔离关系逐项补方向、数量、比例和生效日期。
- 确认 v1.5 查询接口未返回的 16 个参考字段是否历史全空；如不是，补充权威字段查询入口。
- 完成生产并行、回滚演练和单独发布审批；在此之前不修改服务器 cron 和生产 DuckDB。
