# v0.16 发布审核

## 结论

v0.16 完成代码和对比层修复，当前仅可用于本地诊断。账本重算和分类历史快照尚未通过发布条件。

## 本次改动

| 项目 | 处理 |
|---|---|
| 版本身份 | 包版本、运行 ID 和运行清单统一记录 v0.16 |
| 分类依据 | 清单增加来源、覆盖来源、快照日期、依据状态和 SHA-256 |
| 发布前检查 | 带日期且不可修改的分类副本未覆盖运行日期时，状态固定为 `DIAGNOSTIC_ONLY_PUBLISH_BLOCKED` |
| 分类对比 | 拆分为各版本实际分类和 v0.16 快照归一两种视图 |
| 对比检查 | 门店合计毛利继续执行 2%差异检查；分类来源完整确认前，大分类只用于查问题 |

## 验证依据

| 校验 | 结果 |
|---|---|
| 自动化测试 | 159 个通过 |
| 本地对比样本 | A3XV，2026-08-03 至 2026-08-04 |
| SKU 销售事实 | 通过 |
| 实际分类标签 | 16 个在售 SKU日期记录差异，作为诊断 |
| 门店日验收金额 | 1 行差异 |
| 门店周毛利差异检查 | 2 个指标超过 2% |
| 新鲜 v0.16 账本 | D-1 预热日 SKU `20000158` 缺销售退回成本依据，运行中止 |
| 服务器写入 | 0 |

对比数据来自本地固定保存的 08-03～04 试算结果和同日期的 v1.5 只读参考数据。它只检查新的对比程序，不表示生产数据已经验收。

## 当前不能发布的原因

1. 补齐 SKU `20000158` 在 2026-08-02 的合法销售退回成本依据。
2. 确认分类平台规则在历史重算日期应怎样生效，并保存覆盖 08-03～04、带日期且不可修改的分类副本。
3. 解释验收金额和门店毛利超限差异。
