# FIX-027：v0.17 加工反冲、关系分流、外部验收优先与数量桥接

## 问题

v0.16 的加工链把成品盘点和原料账面余额作为放行条件。2026-08-06～11 的旧逻辑会使大量已有销售/报损的成品不生成加工成本；同时，生熟联动原料报损与加工可能命中同一原料日。另有少量同码验收行只有采购金额、没有采购数量，导致门店日验收金额与 v1.5 不一致。

## 最小修复

1. 加工量改为 `成品净销售 + 成品普通报损`，不读取成品盘点。
2. 删除 `v014_stage_raw_available` 和二次基础账本预跑；加工不再因原料账面余额不足停算。
3. 只允许 `compose_out` 透支；BOM、换码和残余转移继续执行严格库存池门禁。
4. 生熟联动与加工二选一，生熟联动优先；新增硬门禁防止重复原料扣减。
5. 删除按同 SPU、连续盘点反推包装的 `facts/pack_inference.py`；不存在当日换码明细表。`purchase_di` 已给 B 时直接外部入账；否则唯一有效 `article_convert` 可用同日 A 验收量直接生成 A OUT/B IN。两种路径二选一。
6. 加工关系增加来源字段、正比例校验、历史版本按日期选择；自环关系明确视为无动作。
7. 同码采购数量缺失时，仅在 `receive_sale` 金额一致的情况下补数量。
8. 同组验收码/销售码盘点冲突时只忽略验收码，保留销售码的有效盘点。
9. 比较口径标签改为版本中性的 `CURRENT_ENGINE_CATEGORY_NORMALIZED`。
10. 若规范化外部验收已对同一门店、日期、A→B 入账正数量，则该路径优先，正式加工关系仅审计、不过账；硬门按完整 A→B 对防止重复。
11. 生效日期、批准状态按关系逐行判断；单条脏关系隔离不再使同一导出中的全部关系失效。

## 不变量

- 加工单事件所有原料转出金额总和等于成品转入金额；
- 内部关系不会改变门店总毛利，只改变 SKU/大分类归属；
- 原料透支成本单列并在毛利中扣回，不伪装为未知报损；
- 外部验收金额只记一次；数量桥接不覆盖已有采购金额；
- 商品集中的 `unit_weight` 可以形成规格比例，但规格比例本身不证明当天发生了转换；只有正式 `article_convert` 才能用 A 验收量直接换码；
- v1.5 结果不进入本地成本和库存计算。

## 2026-08-06～11 完整运行结果

数据范围：A3XV，2026-08-05 预热，08-06～11 发布；本地 Hive 镜像、经营平台当前加工关系、只读 v1.5 对比；服务器写入 0。

| 项目 | 结果 |
|---|---:|
| 输出行数 | 88,996 |
| 正式加工关系 | 69 对、414 个关系日，缺/非法比例 0 |
| 正式加工事件 | 188 个 |
| 加工转出/转入金额 | 6,519.06 / 6,519.06 |
| 外部验收优先 | 6 个成品日，1 对关系，32 件 / 658.08 元 |
| 固定关系直接换码 | 46 个事件、10 对；A OUT 432.648、B IN 692.061、金额 2,978.86 元 |
| 生熟联动优先隔离 | 17 个成品日，涉及 12 个成品 |
| 多配方语义不明隔离 | 24 个成品日，4 个成品 |
| 加工自环无动作 | 162 个关系日，27 对 |
| 账本硬门禁 | 10/10 通过 |
| SKU 日销售额/量 vs v1.5 | 完全一致 |
| 门店日验收金额 vs v1.5 | 6/6 日一致，最大误差 0.0001 |
| 缺成本证据 | 193 个 SKU 日、52 个 SKU；全部无可用同日正成本证据 |
| 分类快照证据 | 经营监控平台最新 2026-08-12 快照；1,449 SKU 命中，11 SKU 回退最新 `dim_goods` |
| 发布状态 | `DIAGNOSTIC_ONLY_PUBLISH_BLOCKED` |

## 毛利影响

同一窗口旧 v0.16 成本回退版门店毛利为 28,392.74，最终 v0.17 为 25,611.95，变化
-2,780.79。主要原因不是内部转移本身——加工/BOM/换码在门店合计金额守恒——而是 v0.17
不再因原料余额不足取消已发生的加工消耗，并把透支成本明确扣回。

固定关系直接换码后，相对 v1.5：门店毛利 -1,734.55（-6.34%），全链路毛利
-1,706.35（-4.91%）。相对仅处理已分配 B 的上一版，门店毛利变化 -9.20 元；内部换码金额
2,978.86 元在门店层完整守恒，差异只来自跨日库存成本轨迹。结果未通过默认 2% 比较门禁，
但不会为贴近 v1.5 删除有证据的换码或恢复重复过账。

详细分类、日期和 SKU 下钻见 [v0.17 发布审核](../reviews/V0.17_RELEASE_REVIEW.md)。

## 验证

- 加工无盘点、有普通报损、原料透支、生熟联动优先、多配方隔离、历史版本、生效期、自环关系均有单元测试；
- 同码验收金额一致补数量、金额不一致继续隔离均有单元测试；
- 外部验收标记入口、同 A 不同 B 反例、同 A→B 硬门均有单元测试；
- 非加工内部流透支有反向测试；
- 最终影子库 10 项硬门禁全部通过；
- 最终比较库精确事实校验全部通过。
